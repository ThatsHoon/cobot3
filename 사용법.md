# rokeyA3_isaac_sim — 사용법 가이드

Isaac Sim + ROS 2 Humble 기반 DMZ 경계초소 자율 정찰 4족 로봇 시뮬레이터.

---

## 환경 정보

| 항목 | 값 |
|---|---|
| Isaac Sim 경로 | `/home/rokey/dev_ws/isaac_sim/isaacsim/_build/linux-x86_64/release` |
| 프로젝트 경로 | `/home/rokey/dev_ws/dmz_sentry` |
| ROS 버전 | ROS 2 Humble |
| ROS_DOMAIN_ID | **123** |

---

## 처음 사용 시 (최초 1회만)

저장소 클론:

```bash
mkdir -p ~/dev_ws
cd ~/dev_ws
git clone https://github.com/ThatsHoon/cobot3 --branch hellokitty --single-branch dmz_sentry
```

---

## 기본 실행

```bash
cd ~/dev_ws/dmz_sentry
./scripts/run_anymal_gp.sh
```

> Isaac Sim GUI가 열리며 DMZ 씬이 로딩됩니다.  
> **첫 실행은 RTX 셰이더 컴파일로 3~5분 소요될 수 있습니다.**

---

## 키보드 조종 (Isaac Sim 창 클릭 후)

| 키 | 동작 |
|---|---|
| `↑` / `↓` | 전진 / 후진 |
| `←` / `→` | 좌/우 횡이동 |
| `N` / `M` | 좌회전 / 우회전 |

---

## 실행 옵션

### 인트루더(침입자) 관련

```bash
# 인트루더 없이 실행
./scripts/run_anymal_gp.sh --no-intruder

# 인트루더 3명, 속도 0.65 m/s
./scripts/run_anymal_gp.sh --intruder-count 3 --intruder-speed 0.65

# Isaac 휴먼 에셋 사용 (없으면 자동 폴백)
./scripts/run_anymal_gp.sh --intruder-visual isaac-human

# 인간 에셋이 뒤를 보면 yaw 보정
./scripts/run_anymal_gp.sh --intruder-visual isaac-human --intruder-yaw-deg 180
```

### 지형 관련

```bash
# 지형 굴곡 높이 조정 (기본 0.28, 낮을수록 평탄)
./scripts/run_anymal_gp.sh --terrain-amplitude 0.20

# 지형 랜덤 시드 변경
./scripts/run_anymal_gp.sh --terrain-seed 11

# 위성/항공사진 텍스처 적용 (실사 느낌)
./scripts/run_anymal_gp.sh \
  --terrain-texture /path/to/orthophoto.png \
  --terrain-texture-scale 1 \
  --no-ground-detail

# PBR 재질 텍스처 적용 (albedo + normal + roughness)
./scripts/run_anymal_gp.sh \
  --terrain-texture /path/to/albedo.jpg \
  --terrain-normal-texture /path/to/normal.jpg \
  --terrain-roughness-texture /path/to/roughness.jpg \
  --terrain-texture-scale 12 \
  --no-ground-detail
```

### ROS 2 센서 관련

```bash
# 모든 ROS 2 센서 비활성화 (카메라, LiDAR, TF, clock)
./scripts/run_anymal_gp.sh --no-ros2-sensors

# LiDAR만 비활성화
./scripts/run_anymal_gp.sh --no-ros2-lidar

# /cmd_vel 수신 비활성화 (키보드 전용 모드)
./scripts/run_anymal_gp.sh --no-ros2-cmd-vel

# /odom 발행 비활성화
./scripts/run_anymal_gp.sh --no-ros2-odom
```

---

## ROS 2 텔레오퍼레이션 (다른 터미널에서)

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=123
ros2 run teleop_twist_keyboard teleop_twist_keyboard --ros-args -p repeat_rate:=20.0
```

---

## ROS 2 토픽 확인 (다른 터미널에서)

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=123
ros2 topic list
```

### 발행되는 주요 토픽

| 토픽 | 내용 |
|---|---|
| `/camera/image_raw` | RGB 카메라 영상 |
| `/camera/depth` | 깊이(Depth) 영상 |
| `/camera/camera_info` | 카메라 캘리브레이션 정보 |
| `/lidar/points` | RTX LiDAR 포인트클라우드 |
| `/odom` | 로봇 오도메트리 |
| `/tf` | 좌표계 트리 |
| `/clock` | 시뮬레이션 시간 |

### 토픽 주파수/데이터 확인

```bash
ros2 topic hz /camera/image_raw
ros2 topic hz /lidar/points
ros2 topic echo /odom --once
```

---

## RViz2 설정

```bash
source /opt/ros/humble/setup.bash
export ROS_DOMAIN_ID=123
rviz2
```

| RViz2 설정 항목 | 값 |
|---|---|
| Fixed Frame | `world` |
| 추가 디스플레이 | Image, PointCloud2, Odometry |

### TF 구조

```
world
  └── base
        └── SentryFrontCamera
  └── SentryLidar  (고정 월드 마운트)
```

---

## YOLO 인트루더 감지 (선택)

### 의존성 설치 (1회)

```bash
python3 -m pip install --user ultralytics
```

### 실행 순서

**터미널 1 — Isaac Sim:**
```bash
cd ~/dev_ws/dmz_sentry
./scripts/run_anymal_gp.sh --no-ros2-lidar
```

**터미널 2 — YOLO 감지기:**
```bash
cd ~/dev_ws/dmz_sentry
./scripts/run_yolo_person_detector.sh --ros-args \
  -p model:=~/dev_ws/dmz_sentry/models/dmz_person_calibration_001_best.pt \
  -p device:="'0'" \
  -p confidence:=0.25 \
  -p image_size:=320 \
  -p publish_annotated:=false \
  -p every_n:=1
```

### YOLO 출력 토픽

| 토픽 | 내용 |
|---|---|
| `/detections_text` | JSON 형식 감지 결과 |
| `/alerts` | 신뢰도 임계값 초과 시 경보 JSON |
| `/camera/annotated` | YOLO 박스가 그려진 카메라 영상 (옵션) |

---

## 지형 미리보기 (로봇/물리/ROS 없음)

로봇 없이 지형만 확인할 때 사용. GPU 사용량 최소화.

### 실행

```bash
cd ~/dev_ws/dmz_sentry
./scripts/run_terrain_preview.sh
```

### 지형 변경 옵션

```bash
# 굴곡 높이 바꾸기 (기본 0.28)
./scripts/run_terrain_preview.sh --terrain-amplitude 0.50

# 랜덤 시드 변경 (지형 모양 자체가 바뀜)
./scripts/run_terrain_preview.sh --terrain-seed 42

# 위성사진 텍스처 입히기
./scripts/run_terrain_preview.sh \
  --terrain-texture /path/to/orthophoto.jpg \
  --terrain-texture-scale 1 \
  --no-ground-detail

# 철조망/프롭 제외하고 지형만
./scripts/run_terrain_preview.sh --no-gp-props --no-external-props --no-ground-detail
```

### 씬 저장 / 불러오기

```bash
# 첫 실행 → 지형 빌드 후 saved_scenes/terrain.usd 에 자동 저장
./scripts/run_terrain_preview.sh

# 다음 실행 → 저장된 파일 자동 로드 (수정 내용 유지)
./scripts/run_terrain_preview.sh

# Isaac Sim 안에서 수정 후 저장: Ctrl+S
# → 같은 파일에 덮어쓰기, 다음 실행 때 반영됨

# 강제로 처음부터 다시 빌드 (저장 파일 무시)
./scripts/run_terrain_preview.sh --rebuild

# 저장 경로 직접 지정
./scripts/run_terrain_preview.sh --save-path ~/dev_ws/dmz_sentry/saved_scenes/my_scene.usd
```

저장 파일 위치: `dmz_sentry/saved_scenes/terrain.usd`

---

### 전체 시뮬과 비교

| | `run_anymal_gp.sh` | `run_terrain_preview.sh` |
|---|---|---|
| 로봇 | ANYmal 4족 포함 | 없음 |
| 물리 엔진 | PhysX 실행 | 없음 |
| ROS 2 | 센서/토픽 발행 | 없음 |
| 렌더러 | 기본값 | RayTracedLighting (가벼움) |
| 용도 | 전체 시뮬레이션 | 지형 디자인 확인용 |

---

## 폴더 구조

```
dmz_sentry/
  isaacsim/          Isaac Sim 스탠드얼론 스크립트
    anymal_gp_terrain.py   전체 시뮬레이션 (로봇+물리+ROS 2)
    terrain_preview.py     지형 미리보기 전용 (GPU 절약)
  scripts/
    run_anymal_gp.sh        전체 시뮬레이션 실행
    run_terrain_preview.sh  지형 미리보기 실행
  assets/            텍스처, 프롭 에셋
  docs/              노트 및 다이어그램
```

---

## 자주 쓰는 명령 모음

```bash
# 전체 시뮬레이션 실행
cd ~/dev_ws/dmz_sentry && ./scripts/run_anymal_gp.sh

# 지형 미리보기 (GPU 절약)
cd ~/dev_ws/dmz_sentry && ./scripts/run_terrain_preview.sh

# ROS 토픽 빠른 확인
source /opt/ros/humble/setup.bash && export ROS_DOMAIN_ID=123 && ros2 topic list

# 텔레오퍼레이션
source /opt/ros/humble/setup.bash && export ROS_DOMAIN_ID=123 && \
  ros2 run teleop_twist_keyboard teleop_twist_keyboard --ros-args -p repeat_rate:=20.0
```
